// Objeto literal

const producto = {
    nombre: "Monitor 20 Pulgadas",
    precio: 300,
    disponible: true,
}

// Object.keys() trae las llaves del objeto
console.log(Object.keys(producto));

// Objetc.values() trae los valores de las llaves del objeto
console.log(Object.values(producto));

// Object.entries te trae todo
console.log(Object.entries(producto));